# 钥匙丢在锁里的那天

## 一则关于自己坑自己的悲伤笑话

我做了一个记账软件。

不只是记账，它还附带了一个看起来很浪漫的功能：写满两千字，就能把这些字"铸"成一张虚拟卡片。卡片有正反面，有卡号，有 QR 码——扫码就能读到藏在里面的文章。更妙的是，如果你选择加密模式，这篇文章会被 AES-256-GCM 加密，连服务器管理员也看不到内容。只有拿着 QR 码的人——那个真正"持有卡片"的人——才能解密阅读。

我觉得这个设计很漂亮。

就像一个真正的保险箱：你把秘密放进去，锁上，然后把钥匙交给你想给的人。服务器只是一个中立的传递者，它甚至不知道自己在传递什么。

然后我就把自己锁在外面了。

事情是这样的。我写了一篇文章，刚好超过两千字，标题和摘要都填好了，正面卡图也上传了。点击"满 2000 字后开卡"，系统生成了卡号和 QR 哈希码。那个瞬间，屏幕上出现了一串长长的十六进制字符串——那就是钥匙。

系统提示我："下载卡片文件并点击开卡结束后，普通界面不再显示这串哈希。"

于是我点了下载。

浏览器弹出了下载提示。我看到了文件名，看到了进度条走完。然后我点了"开卡结束并锁定"。

然后文件没有出现在桌面上。

我翻了下载文件夹。没有。我搜了文件名。没有。我看了浏览器的下载历史——那里写着"已下载"，但文件就是不在。

可能是浏览器的安全策略把 blob 下载拦了。可能是临时目录被清了。可能是硬盘满了。也可能只是运气不好。但结果是一样的：卡片锁了，哈希码消失了，文章还在数据库里——加密的、完整的、永远无法解密的一堆乱码。

讽刺的点在于，这一切都是我自己设计的。

是我写的加密逻辑，是我设定的"锁定后不可查看"规则，是我在代码里写的那行 `qr_locked = 1`。每一个环节都是我亲手打造的。我就像一个锁匠，精心打造了一把完美的锁，然后把自己唯一的一把钥匙锁在了里面。

更讽刺的是，我写的那篇文章，恰好是关于"拥有"和"失去"的。

我记得大概的内容：我们以为自己拥有的东西，其实只是暂时借来的。数据会腐烂，硬盘会坏，密码会忘，服务会关停。所谓"永久存储"不过是人类的一厢情愿。我当时还引用了博尔赫斯的句子——"天堂应该是图书馆的模样"——然后反驳说，不，天堂应该是一个你永远不会忘记密码的地方。

现在回头看，这篇文章与其说是一篇杂文，不如说是一则预言。

写它的时候，我坐在电脑前，泡了一杯茶，手指在键盘上敲得飞快。两千字对于一个经常写东西的人来说不算多，大概四十分钟就写完了。我那时候想的是"这个加密功能真酷"，完全没有意识到几个小时之后，我就会成为它的第一个受害者。

说起来，这不是我第一次被自己的设计坑到。

上大学的时候，我写过一个课程表应用。为了省事，我把所有数据存在浏览器的 localStorage 里。没有云端同步，没有导出功能。理由是"反正是给自己用的"。然后有一天浏览器崩溃了，所有数据都没了。那个学期的课表、作业截止日期、考试时间——全都消失在了一个 corrupted profile 里。

还有一次，我给家里装了一个智能门锁。为了安全，我设置了一个很长的管理员密码，并且没有录入指纹——因为我觉得指纹可以被复制。然后有一天，门锁的键盘失灵了。我只能从阳台翻进去。

每一次都是同一个模式：我为了安全或者优雅，加了一层复杂度，然后这层复杂度在某个意想不到的时刻反咬我一口。

但这次的写作卡加密，可以说是登峰造极了。

因为这次不是"不方便"——是真真切切的"永远丢失"。AES-256-GCM 是军用级加密，量子计算机也破解不了。没有密钥，那些文字就永远锁在那 256 位熵的后面，直到宇宙热寂。

我甚至想过要不要去数据库里把加密数据和 IV 捞出来，写个脚本暴力破解。但我很快就放弃了这个念头。SHA-256 的 QR 密钥空间是 2 的 256 次方。即使我有全世界所有的算力，在太阳烧完之前也跑不完。

所以我只能接受这个事实：我写了一篇文章，它存在于某个地方，以一种我无法触及的方式。它既是存在的，又是不存在的。薛定谔的文章。

后来我给系统加了一些保护措施。下载的 ZIP 包里会附带一个 qr-hash.txt，里面有明文的哈希码。锁定前必须手动勾选"我已确认文件保存成功"。还有一个"复制哈希码"按钮，方便手动备份。

这些都是好改进。但它们改不了已经发生的事。那篇文章还是锁在数字世界里，像一个漂流瓶，等着一个永远不会出现的扫码者。

朋友说这不就是人生吗。

你做了一些选择，当时觉得很好，很正确。后来回头看，发现那些选择把你带到了一个你从未想去的地方。但你已经不能回头了——就像那行 `qr_locked = 1` 一样，一旦设置，就永久生效。

我有时候会想，如果有一天，有人用某种我无法想象的方式把那篇文章解出来了，他们会读到什么。大概是一些关于记忆和遗忘的碎碎念，一些对数字时代"所有权"的质疑，还有博尔赫斯的那句被曲解的名言。

不知道他们会不会觉得好笑。

一个写文章讨论"我们其实什么都留不住"的人，用自己的加密系统，完美地验证了自己的论点。

这大概是最彻底的言行一致了。

所以如果你正在读这篇文章——不管是通过 QR 扫码还是别的什么方式——请你记住：这串哈希码来之不易。备份它。备份你的备份。然后，也许，对"失去"这件事多一些敬畏。

因为你永远不知道，哪一次点击"完成"之后，有些东西就真的回不来了。

---

*写于丢失第一张加密写作卡之后。卡号已不可考，文章标题已遗忘。唯有 256 位的缺席，在数据库里安静地占据着空间。*

---

## 后记

这篇文章的诞生本身就是对主题的呼应。

写它的原因是，我给记账软件加了 API token 功能，让 AI 也能调接口开卡。为了测试这个功能，我需要一篇刚好超过两千字的文章。于是这篇文章被写了出来，通过 HTTPS 请求发送到服务器，用随机生成的密钥加密，然后永久锁定。

但这一次不一样。

这一次，密钥被记录了下来。它会出现在脚本的输出里，出现在这篇 Markdown 文件的元数据里，出现在每一个读到这段话的人面前。它不再是"丢失的钥匙"——它是一把公开展示的钥匙。任何人都可以拿着它去读卡界面扫码，读到一个关于"丢失"的故事。

这大概就是技术迭代的意义：你可以犯一次错，然后修好系统，确保同样的错误不会再发生。但那个最初的错误——那个已经锁在数据库深处的、永远无法被读到的第一篇文章——它会一直留在那里，像一个纪念碑。

提醒你：再完美的系统，也经不起一次手滑。

也提醒你：备份不是可选的。备份是一种生存策略。

如果你读到这里，谢谢你。现在去检查一下你的备份吧。
